local M = {}

M.copilot = {
  suggestion = {
    auto_trigger = true,
    keymap = {
      accept = "<C-y>",
      dismiss = "<C-n>",
      next = "<C-j>",
      prev = "<C-k>"
    }
  }
}


return M
